/* Write a function that returns both the minimum and maximum number of the given list/array. */

function minMax(arr){
    // P -> array of integers
    // R -> array containing min, max
    
    return [Math.min(...arr), Math.max(...arr)]
  }